export interface Fields {
    length30?: string|null
    length10?: string|null
    length3?: string|null
    regex?: string|null
}

export interface ValidationData {
    negative:boolean;
    type: string;
    value: any;
}
